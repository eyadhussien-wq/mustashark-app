# P3.1-J — Graph Evidence Closure Report

**Date:** 2026-09-02  
**Phase:** Evidence Closure / Audit only  
**Branch:** `construction/lawyer-os-v1-p1-audit-2026-09`  
**Inspection head:** `f75e81aa5293bed5f3ec6ce9879f757bcd3a2953`  
**Production DB:** prohibited  
**Financial / Marketplace execution:** prohibited  
**Main:** untouched

## 1. Purpose

This artifact records the branch-pinned evidence closure pass for:

- Transitive dependency closure;
- TypeScript/workspace resolution;
- package export surfaces;
- schema/barrel re-exports;
- Neutral-to-Financial reachability;
- Financial-to-Neutral reverse dependency checks on the inspected Financial schema surface;
- relevant cycle checks on the inspected cross-domain schema graph.

No executable application code or Static Guard is implemented by this artifact.

## 2. Conclusive Finding — Neutral → Financial Schema Transitive Reachability

A repository-wide source-level transitive dependency violation is positively established through branch-pinned source evidence.

The Neutral services import the public `@workspace/db/schema` entry point. The DB package declares that entry point as `./src/schema/index.ts`. The schema barrel then re-exports both Neutral schemas and Financial schemas from the same public surface.

Evidence chain:

`Neutral service → @workspace/db/schema → lib/db/src/schema/index.ts → Financial schema modules`

### 2.1 Neutral import edge

`neutralDocumentAuthorization.ts` imports multiple tables from `@workspace/db/schema`; the Neutral matter/ownership services use the same public schema surface.

Branch-pinned source evidence: `neutralDocumentAuthorization.ts`, `neutralMatters.ts`, `lawyerClientOwnership.ts`.

### 2.2 Resolution edge

`lib/db/package.json` exposes:

- `.` → `./src/index.ts`
- `./schema` → `./src/schema/index.ts`

Therefore `@workspace/db/schema` resolves to the schema barrel rather than to a Neutral-only schema entry point.

### 2.3 Export/barrel edge

`lib/db/src/schema/index.ts` re-exports Financial modules including:

- `./platformDues`
- `./representationFinance`
- `./paymentProofs`
- `./lawyerProposals`
- `./representationQuoteRequests`

The same barrel also exports the Neutral modules.

This makes the Financial schema modules transitively reachable from any Neutral module importing `@workspace/db/schema`.

### 2.4 Financial nature of the reachable modules

`platformDues.ts` contains platform commission authority and financial amount/status fields, including `PLATFORM_COMMISSION_RATE`, gross amount, commission rate, commission amount, and collection status.

`representationFinance.ts` contains representation quotes, milestones, escrow accounts, escrow transactions, commission tiers, lawyer wallets, wallet transactions, and related financial state.

These are therefore identifiable Financial authority/schema surfaces.

## 3. Security Interpretation

This is **not** a claim that Neutral authorization currently reads or mutates financial state. The inspected Neutral functions do not select or use those Financial tables for their authorization decisions.

The conclusive finding is architectural:

> The current public schema barrel creates a real transitive Neutral → Financial dependency/reachability path.

Therefore the Neutral dependency boundary cannot be marked GREEN while the shared barrel remains the resolution path for Neutral services.

## 4. Resolution Graph Result

**Resolution Graph: CONCLUSIVE — boundary leakage established.**

The evidence is branch-pinned and does not depend on default-branch GitHub code search:

1. Neutral source imports `@workspace/db/schema`.
2. `@workspace/db/package.json` resolves that subpath to `src/schema/index.ts`.
3. `src/schema/index.ts` explicitly re-exports Financial schemas.
4. Financial schema files are branch-pinned and identifiable by their financial authority/state fields.

No unresolved alias ambiguity is required to establish this edge.

## 5. Export Surface Result

**Export Surface: CONCLUSIVE — shared barrel is not domain-isolated.**

The public schema barrel combines Neutral and Financial schema exports. Consequently, it is not a Neutral-safe export boundary.

A future Static Guard must reason about resolved export graphs rather than only direct textual imports.

## 6. Transitive Closure Result

**Transitive Closure: CONCLUSIVE NEGATIVE for the required invariant.**

Required invariant:

> No Neutral authority surface may have a reachable dependency path terminating in a Financial or Marketplace authority module.

Observed counterexamples:

`neutralDocumentAuthorization → @workspace/db/schema → schema/index.ts → platformDues`

`neutralDocumentAuthorization → @workspace/db/schema → schema/index.ts → representationFinance`

Thus the closure invariant currently **FAILS**.

## 7. Reverse Dependency Result — Inspected Financial Schema Surface

The Financial schema modules inspected in this closure pass were checked for direct imports of Neutral schema modules.

Observed branch-pinned edges:

- `platformDues.ts` → `bookings`, `offices`, `users`;
- `representationFinance.ts` → `users`;
- `paymentProofs.ts` → `bookings`, `users`;
- `lawyerProposals.ts` → `users`, `representationQuoteRequests`, `representationFinance`;
- `representationQuoteRequests.ts` → `users`, `representationFinance`.

No direct Financial-schema → Neutral-schema import was observed in this inspected surface.

**Result:**

- Financial → Neutral reverse dependency on the inspected schema surface: **NOT OBSERVED**;
- repository-wide reverse dependency absence: **NOT YET CONCLUSIVE**.

This distinction is deliberate: absence from the inspected set is not converted into a repository-wide proof.

## 8. Cycle Result — Inspected Cross-Domain Schema Graph

The inspected cross-domain schema graph has the following relevant direction:

`Neutral services → public schema barrel → Financial schema modules`

Within the inspected Financial schema subset, the observed dependencies terminate in shared/user or Financial schema modules. No path returning from those inspected Financial modules into Neutral schema modules was established.

Therefore:

- Neutral ↔ Financial cycle on the inspected schema surface: **NOT OBSERVED**;
- repository-wide cycle absence: **NOT YET CONCLUSIVE**.

No inference is made from this limited cycle result to the entire repository.

## 9. Data Boundary Status

The Neutral executable services inspected continue to select Neutral identity, relationship, matter, document, share, and audit data rather than financial balances or settlement state.

However, the public schema barrel exposes Financial schema types/modules to the Neutral dependency graph. Therefore:

- data-selection isolation at the inspected service level: **POSITIVE**;
- module/export-level isolation: **FAILED**;
- repository-wide data-flow non-interference: **NOT PROVEN**.

## 10. Runtime / Dynamic Edge Status

Static source and package-resolution evidence establishes the schema leakage. A repository-wide proof of all runtime/dynamic edges (dynamic imports, generated loaders, runtime dependency injection, build-time alias rewrites, or equivalent mechanisms) has not been established in this pass.

Therefore:

- statically resolved cross-domain edge: **PROVEN**;
- repository-wide runtime no-escape proof: **NOT PROVEN**.

This does not weaken the proven static defect; it identifies the remaining proof scope.

## 11. Severity

**F-J-GRAPH-01 — Shared DB schema barrel crosses the Neutral/Financial boundary**

**Severity: High architectural/security boundary defect.**

Rationale:

- It is a real, branch-pinned transitive dependency path.
- It weakens the intended domain isolation even though current Neutral functions do not consume Financial state.
- It makes the existing two-file Static Guard insufficient as a complete boundary control.
- It creates future authority-escalation risk because Financial schema exports are available through the same public import surface used by Neutral authorization code.

No evidence in this pass establishes a direct financial authorization bypass or production data mutation.

## 12. Required Remediation Gate — Design Only

No remediation is implemented in this evidence phase.

Before P3.1-J can become GREEN, the architecture must provide a resolved Neutral-safe schema entry point that does not transitively expose Financial/Marketplace schema modules, or otherwise prove an equivalent isolation mechanism.

The eventual Guard Design must test the **resolved graph**, including:

- package exports;
- barrel exports;
- workspace aliases;
- transitive module reachability;
- reverse dependencies;
- cycles;
- dynamic/runtime edges where relevant.

## 13. Evidence Closure Matrix — Current State

| Dimension | Status | Security meaning |
|---|---|---|
| Source Graph | 🟡 | Principal Neutral/Financial edges identified; repository-wide edge inventory not mechanically materialized here |
| Resolution Graph | 🔴 | `@workspace/db/schema` resolves to shared barrel that exposes Financial modules |
| Export/Barrel Surface | 🔴 | Shared barrel is not domain-isolated |
| Neutral → Financial Transitive Closure | 🔴 | Proven counterexample exists |
| Financial → Neutral Reverse Dependency | 🟡 | Not observed on inspected Financial schema surface; repo-wide absence not proven |
| Cross-Domain Cycles | 🟡 | None observed on inspected schema surface; repo-wide absence not proven |
| Data Selection Isolation | 🟢 | Inspected Neutral authorization queries remain domain-specific |
| Repository-wide Data-flow Non-interference | 🟡 | Not proven |
| Static No-Escape | 🔴 | Fails because the public schema export surface provides an escape path |
| Runtime/Dynamic No-Escape | 🟡 | Not fully proven |
| Production DB Safety | 🟢 | No production connection/write used |
| Financial/Marketplace Execution | 🟢 | Not invoked |
| Main | 🟢 | Untouched |

## 14. Evidence Discipline / Limits

This phase intentionally does **not** convert tool/search limitations into security claims.

In particular:

- default-branch GitHub code search is not treated as branch-pinned proof;
- absence of an observed reverse edge is not treated as repository-wide absence unless all relevant branch-pinned sources are covered;
- absence of an observed cycle is not treated as a mathematical repository-wide DAG proof without a complete import graph;
- CI success is treated as behavioral evidence, not as proof of architectural graph isolation.

The positive Neutral → Financial reachability finding is conclusive because its complete edge chain is branch-pinned and independently resolved through the package export contract.

## 15. Evidence Closure Decision

The principal transitive-closure question is now conclusively decided **NEGATIVE** for the required invariant.

The remaining graph dimensions have been narrowed to explicit proof scopes rather than being guessed closed.

**P3.1-J remains AMBER/FAIL-CLOSED.**

It must **not** advance to Static Guard implementation or GREEN certification until the identified cross-domain export/reachability defect is resolved and the resulting graph is re-audited.

The correct sequence remains:

`Evidence Closure → Boundary Specification → Guard Design → Remediation/Implementation → CI Proof`

## 16. Safety Invariants Preserved

- `main`: untouched.
- Production DB: no connection and no writes.
- Financial/Marketplace execution: not invoked.
- No executable application code changed by this artifact.
- No Static Guard implemented by this artifact.
