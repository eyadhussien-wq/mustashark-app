# P3.1-N — Remediation Preconditions Verification — 2026-09

**Status:** FAIL-CLOSED / REMEDIATION NOT ELIGIBLE  
**Branch:** `construction/lawyer-os-v1-p1-audit-2026-09`  
**Pinned baseline:** `bf119d9f6779d0da60cad37350a592660533a749`  
**Governing document:** `P3.1-N-REMEDIATION-PRECONDITIONS-2026-09.md`

## 1. Scope

This artifact records the first verification pass against the mandatory P3.1-N remediation preconditions. It is evidence collection only.

No schema/package export was changed. No runtime authorization logic was changed. No migration was executed. No Production DB was accessed. `main` was not modified.

## 2. Governing decision rule

```text
PROVEN SAFE        -> eligible for review
PROVEN VIOLATION   -> remediation required; no authorization
UNKNOWN / UNPROVEN -> FAIL-CLOSED
```

The current repository state contains a previously proven Neutral -> Financial boundary violation. Therefore this verification pass cannot produce remediation eligibility unless every mandatory condition is independently proven.

## 3. Baseline identity

The branch resolves to commit:

`bf119d9f6779d0da60cad37350a592660533a749`

The recursive repository tree for this ref is branch-pinned and reports `truncated: false`.

Relevant baseline files and identities:

| Surface | SHA | Finding |
|---|---|---|
| `lib/db/package.json` | `029a3a4431f4bf365611063c3208d28fc94813b8` | public `./schema` export exists |
| `lib/db/src/schema/index.ts` | `bef8916fcb9bd4dd4a973ca7e6d2381ae5d2f677` | combined Neutral + Financial/Marketplace barrel |
| `tsconfig.base.json` | `0d23073d3770dec1286f7c1bbd6eab24fd915fbe` | `moduleResolution: bundler`, custom condition `workspace` |
| `artifacts/api-server/build.mjs` | `86ebf7faaa9322b1ab55cdbbda7febaad27b35cd` | `createRequire()` / runtime `require` capability |

## 4. Current export-surface finding

`@workspace/db` explicitly publishes:

```json
"exports": {
  ".": "./src/index.ts",
  "./schema": "./src/schema/index.ts"
}
```

The schema barrel re-exports Neutral surfaces such as `lawyerClients`, `neutralMatters`, `neutralDocuments`, and `neutralAuditEvents`, while also re-exporting Financial/Marketplace surfaces including `platformDues`, `representationFinance`, `representationQuoteRequests`, `lawyerProposals`, `paymentProofs`, and agreement/legal-representation surfaces.

This means the combined public schema surface remains present at the pinned baseline.

**Result:** PROVEN EXISTING VIOLATION — this is the known P3.1-J defect and is not treated as remediation proof.

## 5. Preconditions matrix

| P3.1-N precondition | Result | Evidence / reason |
|---|---|---|
| 1. Domain ownership | FAIL-CLOSED / incomplete | The pinned tree proves the affected modules exist, but no complete machine-readable domain classification manifest for the protected repository surface is present in the baseline evidence package. |
| 2. Dedicated public surfaces | FAIL-CLOSED | Current package exposes one combined `./schema` surface; future separated surfaces have not yet been implemented or proven. |
| 3. Shared primitive boundary | FAIL-CLOSED | No exact shared-approved allowlist with complete transitive proof is established. |
| 4. Resolver contract | PARTIAL / FAIL-CLOSED | TypeScript uses `bundler` resolution and custom condition `workspace`; package exports are known. Full effective resolver proof across all supported consumers is not yet established. |
| 5. Baseline graph evidence | PROVEN for known negative path / incomplete overall | The known combined-barrel path is branch-pinned. Complete repository-wide protected graph closure is not yet proven. |
| 6. Forward closure | FAIL-CLOSED | Complete Neutral reachable closure through all package/export/barrel/runtime/declaration paths is not proven. |
| 7. Reverse closure | FAIL-CLOSED | Repository-wide Financial/Marketplace -> Neutral authorization implementation absence is not exhaustively proven. |
| 8. Cycle analysis | FAIL-CLOSED | No complete SCC/cycle analysis of the protected graph has been produced. |
| 9. Runtime escape analysis | FAIL-CLOSED | Build configuration explicitly creates `globalThis.require` through `createRequire()`. Absence of protected-domain dynamic runtime loading has not been exhaustively proven. |
| 10. Declaration/build surfaces | FAIL-CLOSED | Full declaration/build graph resolution has not been exhaustively proven. |
| 11. Consumer inventory | FAIL-CLOSED | Repository-wide consumer inventory based on resolved identities, rather than text search alone, is not complete. |
| 12. Remediation invariants | PROVEN AS POLICY / NOT PROVEN BY IMPLEMENTATION | P3.1-K/P3.1-N define the invariants, but the current combined export surface still violates Neutral -> Financial reachability. |
| 13. Regression fixtures | FAIL-CLOSED | The mandatory future resolver-aware negative/positive fixture suite is specified by P3.1-N but is not yet implemented as the authorized guard. |
| 14. Non-interference | PROVEN FOR THIS AUDIT PASS | This pass is documentation/read-only analysis only; no financial behavior, auth logic, migration, Production DB, or `main` was changed. |
| 15. Migration separation | PROVEN AS GOVERNANCE RULE | P3.1-N explicitly separates package/export remediation from DB migration. No migration was executed. |
| 16. CI isolation | FAIL-CLOSED | Existing CI is present, but the resolver-aware P3.1-M guard is not implemented/activated. Existing static CI cannot establish full graph safety. |
| 17. Rollback | PROVEN AS GOVERNANCE REQUIREMENT / implementation not applicable | Remediation has not started; therefore no remediation commit exists that requires rollback. P3.1-N requires an isolated construction-branch rollback point before implementation. |

## 6. Resolver evidence

The pinned TypeScript base configuration contains:

```text
module = esnext
moduleResolution = bundler
customConditions = ["workspace"]
```

The DB package publishes a package-level `./schema` export. Therefore future graph evidence must resolve package exports and the `workspace` condition rather than assuming filesystem-relative semantics.

**Decision:** resolver contract is only partially evidenced; full precondition remains FAIL-CLOSED.

## 7. Runtime escape evidence

The pinned API build configuration imports `createRequire` from `node:module` and assigns:

```text
globalThis.require = createRequire(import.meta.url)
```

It also emits a banner that recreates `globalThis.require` in the generated ESM artifact.

This proves that a runtime `require` capability exists in the build envelope. It does **not** prove that a protected Neutral/Financial target is dynamically loaded, but it prevents us from declaring the absence of such escape paths without further analysis.

**Decision:** UNKNOWN / FAIL-CLOSED for runtime escape precondition.

## 8. Known negative baseline remains reproducible

The current schema barrel contains both Neutral and Financial/Marketplace exports. The previously established P3.1-J path remains structurally available:

```text
Neutral service
  -> @workspace/db/schema
  -> lib/db/src/schema/index.ts
  -> Financial schema
```

The `representationFinance` path remains a mandatory negative regression baseline.

**Decision:** PROVEN VIOLATION, as expected for the pre-remediation baseline.

## 9. Safety boundary confirmation

This verification pass did **not**:

- modify `lib/db/src/schema/index.ts`;
- modify `lib/db/package.json`;
- modify Financial/Marketplace services;
- modify authentication/authorization behavior;
- add or alter migrations;
- access or mutate Production DB;
- activate a new CI guard;
- modify `main`.

## 10. Eligibility decision

```text
P3.1-N Preconditions Verification  🔴 FAIL-CLOSED
Known P3.1-J Defect              🔴 PROVEN / OPEN
Forward Closure                  🔴 UNPROVEN
Reverse Closure                  🔴 UNPROVEN
Cycle Analysis                   🔴 UNPROVEN
Runtime Escape Analysis          🔴 UNPROVEN
Declaration/Build Closure        🔴 UNPROVEN
Consumer Inventory               🔴 UNPROVEN
Resolver Contract                🟠 PARTIAL
Remediation Eligibility          🚫 NO
Implementation                   🚫 BLOCKED
Migration                        🚫 BLOCKED
CI Activation                    🚫 BLOCKED
Production DB                    🔒 FROZEN / UNTOUCHED
main                             🔒 FROZEN / UNTOUCHED
```

## 11. Next safe verification objective

The next action remains evidence-only: produce the missing branch-pinned protected graph evidence required by P3.1-N, prioritizing:

1. complete domain classification manifest;
2. exact shared-approved allowlist;
3. effective resolver contract;
4. forward Neutral closure;
5. reverse Financial/Marketplace closure into Neutral authorization;
6. SCC/cycle analysis;
7. runtime escape inventory;
8. declaration/build surface inventory;
9. resolved consumer inventory.

No implementation is authorized by this document.

## 12. Final rule

Until all mandatory P3.1-N preconditions are proven and independently reviewed:

```text
SAFE HARBOR REMAINS ACTIVE.
```
