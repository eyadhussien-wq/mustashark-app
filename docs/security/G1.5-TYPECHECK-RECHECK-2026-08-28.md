# G1.5 Typecheck Recheck

The booking controller must use the safe booking query module and must not import the removed `cancelBooking` export from `safeCancelBooking`.

Canonical import:

```ts
import { listMyBookingsSafe, getBookingByIdSafe } from "./safeBookingQueries";
```

This commit intentionally contains no financial-logic changes. It records the canonical state so the G1.5 gate is re-evaluated from a fresh commit after the stale TypeScript failure.
