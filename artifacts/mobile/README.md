# Mustasharek Mobile

Expo Go mobile application for Mustasharek.

## Run locally

From the repository root:

```bash
pnpm install
cd artifacts/mobile
pnpm start
```

Then scan the QR code with Expo Go.

This mobile app is intentionally isolated from the existing web/API packages while sharing the same repository and future API contracts.
